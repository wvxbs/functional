var MultiplyArray = []

export const Multiplier = () => {
    const Ops = [181, 82, 192, 104, 65, 75, 57, 156, 92, 69, 147, 142, 184, 134, 155]

    return OperationArray(Ops)

}

const OperationArray = Ops => { 
    Ops.map(item => {
        SequenceArr = []
        Sequence(item)
        resultsArray.push(${MultiplyArray())
    })

    return resultsArray

}

const Sequence = (Item) => {
    if (i > 0){

    }
    
}